unit unEstoque;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, unCadPadrao, ActnList, StdCtrls, Buttons, ExtCtrls;

type
  TF_cadPadrao1 = class(TF_cadPadrao)
    procedure BitBtn5Click(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  F_cadPadrao1: TF_cadPadrao1;

implementation

{$R *.dfm}

procedure TF_cadPadrao1.BitBtn5Click(Sender: TObject);
begin
  inherited;
  close;
end;

end.
